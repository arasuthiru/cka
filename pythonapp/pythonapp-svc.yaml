apiVersion: v1
kind: Service
metadata:
  name: python-hello-service
spec:
  selector:
    app: python-hello
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer # Or NodePort for local testing